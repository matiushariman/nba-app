const InfoBanner = () => (
  <div className="bg-green-800">
    <div className="container mx-auto pt-2 pb-2">
      <p className="text-center text-white text-sm">
        The data shown below is currently mocked (Live data is available in
        development mode only due to server hosting issue).
      </p>
    </div>
  </div>
);

export default InfoBanner;
